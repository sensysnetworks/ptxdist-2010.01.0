# /etc/network/interfaces

# The loopback interface
auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp
#	pre-up nameif $IFACE 00:E0:81:25:02:EF
#	up echo 1 > /proc/sys/net/ipv4/ip_forward

